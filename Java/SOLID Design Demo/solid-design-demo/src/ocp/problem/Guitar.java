package ocp.problem;

// let's suppose in the feature if we want to add the new cool flame pattern to make it look more rock and roll.
// then we have to change the below Guitar class
public class Guitar {
    private String make;
    private String model;
    private int volume;
}
